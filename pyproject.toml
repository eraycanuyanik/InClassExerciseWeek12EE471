[tool.ruff]
line-length = 100
target-version = "py312"
extend-exclude = [
    ".venv",
    "**/migrations/**",
    "djangotutorial/db.sqlite3",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]
